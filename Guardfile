# Compile coffeescript to javascript with Guard
# More info at https://github.com/guard/guard#readme

guard 'coffeescript', :input => "src", :output => "lib"
guard 'coffeescript', :input => "spec/src", :output => "spec/lib"
